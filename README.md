# Portafolio - Esqueleto limpio

Este repositorio ha sido reinicializado con un esqueleto mínimo de Next.js + Tailwind.

Pasos rápidos para ejecutar localmente:

```powershell
pnpm install
pnpm dev
```

Si no usas pnpm, usa `npm install` y `npm run dev`.
